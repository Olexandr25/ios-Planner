import styled from "styled-components"

export const DividerStyled = styled.div`
  width: inherit;
  display: flex;
  border-bottom: 1px solid var(--color-gray-darken-1);
  margin-left: 8px;
  padding-bottom: 4px;
`
